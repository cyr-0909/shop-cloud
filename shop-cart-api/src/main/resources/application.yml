server:
  port: 8086
eureka:
  client:
    register-with-eureka: true #注册到Eureka的注册中心
    fetch-registry: true #获取注册实例列表
    service-url:
      defaultZone: http://p1:8000/eureka/,http://p2:8111/eureka/,http://p3:8222/eureka/ #配置注册中心地址
spring:
  application:
    name: shop-cart-api #配置注册中心地址
count:
  limit: 10